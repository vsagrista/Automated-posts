== README

App built with rails using several gems: omniauth, twitter, font awesome, bootstrap, device.

- Git clone, cd to file, run bundle install
- Run rake db:migrate
- Run rails sever and rake jobs:work (the jobs on new tab)

